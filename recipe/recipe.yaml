# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.2.8"

package:
  name: xorq-datafusion
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/x/xorq-datafusion/xorq_datafusion-${{ version }}.tar.gz
  sha256: 531ad0c0fb2fc8e547a11d6716d6739b8df352edec0dfa9e409f703cbae36693

build:
  number: 1
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      #CARGO_PROFILE_RELEASE_LTO: fat # note time out issues
      PYTHONUTF8: "1"
    content:
      - if: unix
        then: |
          cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
          ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
        else: |
          cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
          %PYTHON% -m pip install . -vv --no-deps --no-build-isolation
  python:
    version_independent: true

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - maturin >=1,<2

    - ${{ compiler("c") }}
    - ${{ stdlib("c") }}
    - ${{ compiler("rust") }}
    - cargo-bundle-licenses
  host:
    - python
    - maturin >=1.7,<2.0
    - pip
  run:
    - python
    - pyarrow >=18.0.0,<22.0.0

tests:
  - python:
      imports:
        - xorq_datafusion
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: xorq Python DataFusion Bindings.
  homepage: https://github.com/xorq-labs/xorq-datafusion
  license: Apache-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - rxm7706
